import { InfiiniteScrollDirective } from './infiinite-scroll.directive';

describe('InfiiniteScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new InfiiniteScrollDirective();
    expect(directive).toBeTruthy();
  });
});

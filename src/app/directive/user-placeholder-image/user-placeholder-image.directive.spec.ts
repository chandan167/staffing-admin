import { UserPlaceholderImageDirective } from './user-placeholder-image.directive';

describe('UserPlaceholderImageDirective', () => {
  it('should create an instance', () => {
    const directive = new UserPlaceholderImageDirective();
    expect(directive).toBeTruthy();
  });
});

import { ImgPreDirective } from './img-pre.directive';

describe('ImgPreDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgPreDirective();
    expect(directive).toBeTruthy();
  });
});
